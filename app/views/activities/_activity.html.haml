%li.activity{:id => ["activity", activity.id]}
  %h3= activity.name
  %h4= activity.location
  = image_tag activity.image_url, alt: activity.name, :class => "bucket-image-small"
  - unless @is_viewer
    = link_to "Remove", activity_path(activity), :method => :delete, :remote => true